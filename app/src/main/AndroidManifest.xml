<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ahmed.tauqeer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".InFixActivity"></activity>
        <activity android:name=".HexToDec" />
        <activity android:name=".DecToHex" />
        <activity android:name=".OctToDec" />
        <activity android:name=".DecToOct" />
        <activity android:name=".BinToDec" />
        <activity android:name=".DecToBin" />
        <activity android:name=".NotationsActivity" />
        <activity android:name=".DigitsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>